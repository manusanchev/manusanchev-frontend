---
import { Image } from "astro:assets";

interface Props {
  image: any;
}

const { image } = Astro.props;
const { url, alt } = image;
---

<Image
  class="rounded-lg mb-4 object-cover"
  width="100"
  height="100"
  loading="eager"
  src={url}
  alt={alt}
/>
