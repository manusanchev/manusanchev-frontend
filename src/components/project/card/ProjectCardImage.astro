---
import { Image } from 'astro:assets';

interface Props {
    image: any;
}

const { image } = Astro.props;
const { url, alt } = image;
---

 <Image class="rounded-lg mb-4 object-cover" width="700" height="500" src={url} alt={alt} />
